pub mod channel;
pub mod config;
pub mod errors;
pub mod place;
pub mod server;

fn main() {
	/*let server = Server::new(ServerConfig {
		port: 8080,
		url: "localhost",
		loki_auth: "localhost:1041",
	});*/
}
